import { styled } from '@mui/material/styles';

const style = {
    Container: styled('div')`
        align-items: center;
        display: flex;
        flex-direction: row;
        gap: 16px;
    `,
};

export default style;
